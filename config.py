# Chunking settings
CHUNK_SIZE = 1024  # Larger chunk size to avoid too many small chunks
CHUNK_OVERLAP = 200  # Overlap for context continuity

# FAISS index persistence path
FAISS_INDEX_PATH = "persistent/faiss_index"